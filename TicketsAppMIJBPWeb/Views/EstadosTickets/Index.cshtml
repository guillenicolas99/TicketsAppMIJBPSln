@model IEnumerable<CapaInfraestructura.CapaDominio.Entities.EstadosTicket>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreEstadoTicket)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionEstadoTicket)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreEstadoTicket)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionEstadoTicket)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEstadoTicket">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEstadoTicket">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEstadoTicket">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
