@model IEnumerable<CapaInfraestructura.CapaDominio.Entities.Rede>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreRed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionRed)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreRed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionRed)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdRed">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdRed">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdRed">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
